输入：样本集D={x1, x2,...,xm}, 聚类簇数k

过程：
从D中随机选择k个样本座位初始均值向量{u1, u2,...,uk}
repeat
    for j=1,2...,m 执行
      计算样本xj与各个均值向量的距离
        根据距离最近的均值向量确定xj属于哪个簇
        将样本xj划入相应的簇
    结束循环


    for i=1,2,...,k 执行
        计算新的均值向量ui
        如果新的均值向量与旧的均值向量不同：
            将旧的均值向量更新为新的
        如果相同：
            不变
    直到当前均值向量都不变

输出：簇划分C={C1,C2,...,Ck}
